# secrets — never commit
.env
.env.*
*.pem
*.key

# OS / editor
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/

# build / deps (none today, but keeps future tooling out)
node_modules/
dist/
.cache/
